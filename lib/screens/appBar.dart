import 'package:flutter/material.dart';

final appBar = AppBar(
  title: Text('Flatform'),
  centerTitle: true,
  actions: <Widget>[
    IconButton(
      icon: Icon(Icons.add),
      onPressed: () => null,
    )
  ],
);
